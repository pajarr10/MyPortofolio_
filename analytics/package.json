{
  "name": "portfolio-analytics",
  "version": "2.0.0",
  "description": "Backend analytics & admin dashboard untuk website portfolio (Upstash Redis + fallback file JSON)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
